package com.company.ftthgis.domain.task.entity;

public enum TaskType {
    /** Customer support ticket — complaint, outage, service disruption. */
    TICKET,

    /** Infrastructure project — cable pull, pole installation, splicing. */
    PROJECT
}
