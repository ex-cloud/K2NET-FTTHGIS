{
  "name": "k2net-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "verify:admin": "bash scripts/verify-all.sh",
    "audit:colors": "grep -rn 'text-zinc-\\|bg-zinc-\\|border-zinc-\\|text-white\\|bg-emerald-\\|text-emerald-\\|border-emerald-' apps/studio-admin/src --include='*.tsx' --include='*.ts' | grep -v 'node_modules'"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@9.15.9"
}
