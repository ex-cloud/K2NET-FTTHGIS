name: Go Gateways CI/CD

on:
  push:
    branches:
      - develop
    paths:
      - 'services/map-gateway/**'
      - 'services/notification-gateway/**'
      - 'services/payment-gateway/**'
      - 'services/storage-gateway/**'
      - 'services/shared/**'
      - 'services/go.work'
      - '.github/workflows/gateways-ci.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target Environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production

jobs:
  build-and-test:
    name: Build & Test Go Services
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [map-gateway, notification-gateway, payment-gateway, storage-gateway]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'
          cache-dependency-path: services/go.work

      - name: Test Service
        run: |
          cd services/${{ matrix.service }}
          go test ./...

      - name: Build Binary
        run: |
          cd services/${{ matrix.service }}
          go build -o bin/service ./cmd/...

  deploy:
    name: Deploy Gateways to Server
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop' || github.event_name == 'workflow_dispatch'
    steps:
      - name: Determine Environment
        id: env-val
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "DEPLOY_ENV=${{ github.event.inputs.environment }}" >> $GITHUB_ENV
          else
            echo "DEPLOY_ENV=staging" >> $GITHUB_ENV
          fi

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ env.DEPLOY_ENV == 'production' && secrets.PROD_SSH_HOST || secrets.STAGING_SSH_HOST }}
          username: ${{ env.DEPLOY_ENV == 'production' && secrets.PROD_SSH_USER || secrets.STAGING_SSH_USER }}
          key: ${{ env.DEPLOY_ENV == 'production' && secrets.PROD_SSH_KEY || secrets.STAGING_SSH_KEY }}
          port: ${{ env.DEPLOY_ENV == 'production' && secrets.PROD_SSH_PORT || secrets.STAGING_SSH_PORT || 22 }}
          script: |
            cd /opt/project5
            git fetch origin
            if [ "${{ env.DEPLOY_ENV }}" = "production" ]; then
              git checkout main
              git pull origin main
            else
              git checkout develop
              git pull origin develop
            fi
            bash scripts/deploy.sh ${{ env.DEPLOY_ENV }} gateways
